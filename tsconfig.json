{
  "extends": "./configurations/configs/typescript/tsconfig.json",
  "compilerOptions": {
    //Modules
    "baseUrl": ".",
    "customConditions": ["./eslint", "./prettier"],
    // Emit
    "noEmit": true,
    "outDir": "dist"
  }
}
